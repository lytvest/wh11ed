---
name: release-frontend
description: Выкатить фронтенд wh11ed в прод (wh-rules.ru) — сверка версии с чейнджлогом, гейты, фоновый деплой, смоук живого домена. Использовать на «задеплой», «выкати», «релиз».
---

# Релиз фронтенда

Деплой сам бампает патч-версию, коммитит и пушит `chore: release vX.Y.Z`. Значит **всё, что
должно уехать, уже должно быть закоммичено**, а рабочее дерево — чистым: `deploy.sh` прерывается
на грязном дереве и на любой ветке кроме `main`.

## 1. Что именно едет

```bash
git log --oneline "$(git log --format=%H --grep='chore: release' -1)"..HEAD
```

Это список изменений с прошлого релиза. По нему решается, нужна ли запись в чейнджлоге.

## 2. Чейнджлог — сверить версию, а не завести новую вслепую

`src/data/changelog.js`, запись `[0]` — самая свежая. Сравнить её `version` с `package.json`:

- **Версии равны** → запись `[0]` уже задеплоена; новая работа = новая запись с **следующим**
  патчем (деплой его и поставит).
- **Версия в чейнджлоге выше** → она ещё не выкатывалась; дописывать **в неё**, а не заводить
  ещё одну. Иначе плашка обновления покажет одну версию, а приложение будет другой.

Как писать саму запись (голос владельца, без техники, EN/RU параллельно) — скил
`changelog-entry`. Тривиальный деплой записи не требует: версия просто сдвинется молча.

## 3. Гейты (все обязаны быть зелёными)

```bash
npm test && npm run lint && npm run build
```

Если трогались данные правил — дополнительно то, что относится к правке: `npm run parity`,
`npm run sync`, `npm run detmeta`, `npm run wtags`, `npm run dsrules`, `npm run imghash`.
Подробности в `src/data/CLAUDE.md` → Data gates.

## 4. Деплой

Долгий (3100+ ключей бакета). **Запускать в фоне и не пайпить в `tail`** — вывод нужен целиком:

```bash
npm run deploy --prefix /путь/к/wh11ed > /tmp/deploy.log 2>&1
```

Фоновая команда стартует в корне рабочей области, поэтому `--prefix` обязателен. `BUMP=none` —
выкатить текущую версию как есть, `BUMP=minor`/`major` — если релиз того стоит.

Если запуск блокируется классификатором — попросить пользователя выполнить команду самому
(`! cd … && bash deploy.sh`), не пытаться обойти.

## 5. Смоук живого домена (без него релиз не закрыт)

```bash
curl -s https://wh-rules.ru/ | grep -o 'assets/index-[a-zA-Z0-9]*\.js' | head -1   # новый бандл
curl -s https://wh-rules.ru/assets/index-XXXX.js | grep -c "X\.Y\.Z"               # чейнджлог внутри
curl -s -o /dev/null -w '%{http_code}\n' https://wh-rules.ru/<новая-страница>
```

В логе деплоя убедиться, что прошли **оба** финальных шага: `CDN purged` и
`Committing + pushing version bump…`. Без purge читатели получат старый `index.html` до часа.
